name: arrow_puzzle
description: A fun and addictive arrow puzzle game built with Flutter.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  webview_flutter: ^4.14.1
  shared_preferences: ^2.5.3
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  assets:
    - assets/arrow-dot-game-42.html
  uses-material-design: true
